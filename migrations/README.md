# Migrations

This folder is intentionally a placeholder.

Do not run GORM `AutoMigrate` or create schema-changing migrations until the
legacy database schema is inspected and an explicit migration approval exists.
The Go API must remain compatible with the legacy schema by default.
